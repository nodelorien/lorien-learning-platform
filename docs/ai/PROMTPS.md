# Starting Initial feature 

The student view:

You have to complete the next features, you have to implements a website with the next components:

login: just add the user: admin and password: Julian.123 for admin webmaster in the db.
registration: a basic form user registration with 3 fields, name, company, and a dropdown list with training option to choose.
home: a ranking of users that completed more execises sucessfully and with less time, Also have to show a topbar to redirect to training module.

training module: the trainign module contains the exercises to resolve and complete, the admin approve and enable or disable the exercises in the training course. The exercises must be in order, and have to store stats of user for each one.

The admin view:

The admin dashboard has the crud for trainings, and trainings has a relationship with exercises, you can add excercises to a training but exist other view to register exercises by topic and category, the admin only drag and drop or add the exercises in the training, the training must have a expiration time and its own stats, number of users registered, and execises completed, the winners of training course.

The exercises view: has a crud to register execises, should be to resolve prompts, or trivia with multiple selection.

the website must have english and spanish, but we are going to start only with spanish.

You have to build the most beautiful training enterprise platform, i recommend material desing for nextjs or tailwind... you have to choose the most beautiful and easy front framework.

Have to implement the .env to save the deepseek api to validate the pusher integration.
